<!DOCTYPE html>

<html lang="en" class="light-style layout-menu-fixed" dir="ltr" data-theme="theme-default" data-assets-path="/"
  data-template="vertical-menu-template-free">

@include('partials/head')

<body>
  <!-- Layout wrapper -->
  <div class="layout-wrapper layout-content-navbar">
    <div class="layout-container">
      <!-- Menu -->
      @include('partials/aside')
      <!-- Layout container -->
      <div class="layout-page">
        <!-- Navbar -->
        @include('partials/header')
        <!-- Content wrapper -->
        <div class="content-wrapper">
          <!-- Content -->
          <div class="container mt-2">
            <div class="card">
              <div class="card-body">
                @if(source=="itemUpdate")
                @include('partials/error/itemUpdateError')
                @elseif(source=="itemDelete")
                @include('partials/error/itemDeleteError')
                @elseif(source=="categoryDelete")
                @include('partials/error/categoryDeleteError')
                @elseif(source=="groupDelete")
                @include('partials/error/groupDeleteError')
                @elseif(source=="userDelete")
                @include('partials/error/userDeleteError')
                @end
                <div class="col-12 mt-4">
                  <a href="javascript:history.back()" class="btn btn-secondary">Regresar</a>
                </div>
              </div>
            </div>
          </div>
        </div>
        <!-- Footer -->
        @include('partials/footer')
      </div>
    </div>
    <!-- Overlay -->
    <div class="layout-overlay layout-menu-toggle"></div>
  </div>

  @include('partials/scripts')

  <!-- ScriptPage -->
  <script src="/pages/user.js"></script>
</body>

</html>