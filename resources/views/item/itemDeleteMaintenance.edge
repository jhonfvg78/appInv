<!DOCTYPE html>

<html lang="en" class="light-style layout-menu-fixed" dir="ltr" data-theme="theme-default" data-assets-path="/"
    data-template="vertical-menu-template-free">

@include('partials/head')

<body>
    <!-- Layout wrapper -->
    <div class="layout-wrapper layout-content-navbar">
        <div class="layout-container">
            <!-- Menu -->
            @include('partials/aside')
            <!-- Layout container -->
            <div class="layout-page">
                <!-- Navbar -->
                @include('partials/header')
                <!-- Content wrapper -->
                <div class="content-wrapper">
                    <!-- Content -->
                    <div class="container mt-3">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">RETIRAR DE MANTENIMIENTO</h5>
                                <div class="mb-3">
                                    <h5 class="modal-title" id="deleteReference">{{item.reference}}</h5>
                                    <h5 class="modal-title" id="deleteId" hidden>{{item.id}}</h5>
                                </div>
                                <div class="mb-3">
                                    <label for="location" class="form-label">VALIDAR REFERENCIA</label>
                                    <input type="text" class="form-control" id="confirm_reference" />
                                    <p><small>Digite la referencia del elemento.</small></p>
                                </div>
                                <form id="deleteForm" action="" method="POST">
                                    <button class="btn btn-danger" type="button" data-bs-toggle="modal"
                                        onclick="deleteItem()">Eliminar</button>
                                    <a href="javascript:history.back()" class="btn btn-secondary">Cancelar</a>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- Footer -->
                @include('partials/footer')
            </div>
        </div>
        <!-- Overlay -->
        <div class="layout-overlay layout-menu-toggle"></div>
    </div>

    @include('partials/scripts')

    <!-- ScriptPage -->
    <script src="/pages/item.js"></script>
</body>

</html>